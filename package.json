{
  "name": "credence",
  "version": "0.1.0",
  "private": true,
  "packageManager": "npm@11.17.0",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "concurrently --kill-others --names api,web \"npm run dev --workspace @credence/api\" \"npm run dev --workspace @credence/web\"",
    "build": "npm run build --workspace @credence/shared && npm run build --workspace @credence/api && npm run build --workspace @credence/web",
    "lint": "npm run lint --workspaces --if-present",
    "test": "npm run test --workspaces --if-present",
    "typecheck": "npm run typecheck --workspaces --if-present"
  },
  "devDependencies": {
    "concurrently": "latest",
    "next": "16.3.4"
  }
}
